# Android
android/.gradle/
android/build/
android/app/build/
android/local.properties
android/.idea/
android/captures/
android/*.iml
android/app/*.iml

# Rust
pc/target/
pc/Cargo.lock

# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
*.swp
*~

# Third-party binaries — do not commit, do not redistribute.
#
# These are other people's builds. Their licenses govern redistribution, and
# this repository is published as a GitHub Pages site, so anything committed
# here is served to the public. Download them yourself; output/README.md says
# where from.
#
# The earlier rule below only covered pc/, so output/wintun.dll slipped through
# and was published for months. Match by name wherever they land.
wintun.dll
tun2proxy
tun2proxy.exe
adb
adb.exe
