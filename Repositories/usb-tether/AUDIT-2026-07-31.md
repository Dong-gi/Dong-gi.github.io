# 문서·주석·코드 정합성 감사 (2026-07-31)

대상: `README.md`, `CLAUDE.md`, 모든 `.kt` 주석/KDoc, `AndroidManifest.xml`, `strings.xml`,
`activity_main.xml`, `*.gradle.kts`, `output/*.bat`, `output/*.sh`.

심각도: **H**(고쳐야 함) / **M**(오해 유발) / **L**(정리 권장)

---

## 1. 주석이 실제 코드와 모순

| # | 심각도 | 위치 | 내용 |
|---|---|---|---|
| 1.1 | **H** | `ConnectionRegistry.kt:37` | KDoc: "`register` … `handleClient` 진입 직후 호출한다". 실제 호출은 `acceptLoop()` 안 `executor.submit` **직전**(`Socks5Server.kt:117`, `HttpProxyServer.kt:123`). 두 서버는 오히려 "등록은 acceptLoop 에서 이미 했다"(`Socks5Server.kt:140`, `HttpProxyServer.kt:146`)라고 반대로 적어 놓았다. 등록 위치가 동시 세션 상한의 정확성을 결정하는 핵심이므로, 이 KDoc을 그대로 믿고 옮기면 스레드 폭주 회귀가 생긴다. |
| 1.2 | **H** | `Socks5Server.kt:229–231` vs `:169` | UDP KDoc이 "DST.ADDR/DST.PORT 는 파싱된 뒤 버려졌다"를 **이전 구현의 문제**로 열거하지만, 현재 코드도 그대로 버린다 — `handleClient`가 파싱한 `host`/`dstPort`는 `handleUdpAssociate(client)`에 전달되지 않고, 클라이언트 포트는 여전히 첫 패킷에서 학습한다(`:353–358`). 고쳐졌다고 읽히는 서술이지만 고쳐지지 않았다. |
| 1.3 | **M** | `HotspotPreferences.kt:95`, `:119` | "56자 알파벳"이라고 두 번 적었지만 `PASSPHRASE_ALPHABET`은 **57자**(소문자 25 + 대문자 24 + 숫자 8, 중복 없음). 결과값 "약 116비트"는 57자 기준이 맞으므로(20·log₂57 = 116.7) **숫자만 틀렸다**. |
| 1.4 | **M** | `WifiHotspot.kt:227` vs `:230` | KDoc: SSID는 `DIRECT-xx-`(xx는 **두 글자**)로 시작해야 한다. 코드는 `startsWith("DIRECT-")`만 본다 → `DIRECT-a`, `DIRECT-` 같은 입력이 규격 위반 상태로 그대로 통과한다. 검사와 서술 중 하나를 맞춰야 한다. |
| 1.5 | **M** | `macos-wifi.sh:12–16` | 주석 `# Auto-elevate` 아래 코드는 재실행하지 않고 `echo "Run with sudo"; exit`로 끝난다. 게다가 `exit`에 코드가 없어 **실패가 종료코드 0**으로 보고된다. |
| 1.6 | **L** | `DestinationFilter.kt:70–71` | KDoc의 차단 목록이 실제 구현과 어긋난다(§4.4 참고 — 255.255.255.255 미차단). |

## 2. 문서 간 / 문서-코드 모순

| # | 심각도 | 위치 | 내용 |
|---|---|---|---|
| 2.1 | **H** | `CLAUDE.md:32` | "`windows-wifi.bat` — it auto-elevates, **probes `192.168.49.1` ports 1080–1089**". 스크립트는 탐색을 제거했고(`windows-wifi.bat` 주석 전체가 그 이유를 설명), 같은 파일 `CLAUDE.md:68`은 "The launcher scripts **no longer probe**"라고 적었다. **한 문서 안에서 정면 충돌**하며, 하필 이번 변경의 핵심 보안 근거를 뒤집는 문장이다. |
| 2.2 | **H** | `CLAUDE.md:28` | "Requires Android SDK **API 34**, Java 17. Uses **AGP 8.7.0 + Kotlin 2.0.20**." 실제: `compileSdk`/`targetSdk` = **37**(`app/build.gradle.kts:9,14`), AGP **9.2.1**(`build.gradle.kts:3`), Gradle **9.4.1**, 데몬 툴체인 **JDK 21**(`gradle-daemon-jvm.properties`). Kotlin 플러그인·버전은 어디에도 선언되지 않았다 — AGP 9의 built-in Kotlin에 의존하므로 코틀린 버전이 **핀 고정되지 않은 상태**다(AGP가 끌어오는 KGP를 그대로 씀). 재현 가능한 빌드를 원하면 별도 고정이 필요하다. |
| 2.3 | **M** | `CLAUDE.md:34`, `:62` | 존재하지 않는 `pc/` 디렉터리를 계속 언급한다(`.gitignore`에도 `pc/` 규칙 잔존). |
| 2.4 | **M** | `README.md:56`, `:78` | 디렉터리 트리에 `output/usb-tether.apk`가 있고 `output\adb.exe install output\usb-tether.apk` 설치 명령까지 안내하지만 **APK가 없다**. 이 명령은 그대로 실패한다. |
| 2.5 | **M** | `README.md:125–130` | macOS 절: "macOS용 바이너리는 `output/`에 포함되어 있지 않으므로 직접 준비해야 합니다 … 압축 해제 후 `output/tun2proxy`로 배치". 실제로 `output/tun2proxy`(Mach-O **arm64**)와 `output/adb`(Mach-O universal)가 **이미 들어 있다**. Apple Silicon 사용자는 하지 않아도 되는 작업을 안내받고, Intel 사용자는 arm64 바이너리가 이미 자리를 차지하고 있다는 사실을 모른다. |
| 2.6 | **M** | `CLAUDE.md:31`, `:80` | `output/` 목록을 `tun2proxy.exe` + `wintun.dll` + `adb.exe`로만 적어 macOS 바이너리 2개와 `macos-wifi.sh`를 누락. `CLAUDE.md`의 "Running End-to-End"에 macOS 경로가 아예 없다. |
| 2.7 | **M** | `CLAUDE.md:50–58` 표, `README.md:43–50` 트리 | 최근 추가된 파일이 문서에 없다 — `ConnectionRegistry.kt`, `SecretStore.kt`, `HotspotStartActivity.kt`, `HotspotTileService.kt`(+ `CLAUDE.md`는 `IpAddress.kt`도 누락). **퀵 설정 타일 기능 자체가 두 문서 어디에도 설명되지 않는다.** `CLAUDE.md:58`의 "MainActivity … Launcher only"는 맞지만, 특권 동작이 어디로 갔는지(= 타일 + 트램폴린)는 적혀 있지 않아 반쪽 서술이다. |
| 2.8 | **M** | `CLAUDE.md:73` | 권한 목록에 `WAKE_LOCK`(코드 미사용, §4.6)이 있고 매니페스트가 선언한 `ACCESS_NETWORK_STATE`는 빠졌다. |
| 2.9 | **M** | `CLAUDE.md:56` | HTTP 프록시를 "with **the same** 10-attempt fallback (**same** `start()` contract)"라고 적었는데, 바로 위 행(`:53`)의 SOCKS5는 "**no port fallback**"이다. "same"이 가리킬 대상이 사라진 **매달린 참조**(폴백을 둘 다 갖고 있던 시절의 잔재). |
| 2.10 | **M** | `AndroidManifest.xml:54–56` | `PROPERTY_SPECIAL_USE_FGS_SUBTYPE` = `"ip_packet_tunnel"`. 이 앱은 IP 패킷을 포워딩하지 않는다 — `README.md:24`가 "핵심 통찰은 폰이 클라이언트의 IP 패킷을 그대로 포워딩하지 **않는다**는 것"이라고 강조하는 바로 그 지점과 충돌한다. 실제로는 사용자 공간 프록시이므로 Play 심사용 문구로도 부정확하다. |
| 2.11 | **L** | `README.md:153` vs `CLAUDE.md:74` | 전자는 "Android 10(API 29) 이상 필요", 후자는 "minimum API 26". 둘 다 부분적으로 맞지만(설치는 26, 핫스팟은 29) 한쪽만 읽으면 어긋난다. 한 문장으로 합치는 편이 낫다. |

## 3. 불완전한 서술 / 근거 없는 단정

| # | 심각도 | 위치 | 내용 |
|---|---|---|---|
| 3.1 | **M** | `README.md:24–26`, `CLAUDE.md:17,46`, `Socks5Server.kt:33–34`, `HttpProxyServer.kt:27–28`, `TetherService.kt:28–29` | "통신사가 보는 트래픽은 **완전히** 폰이 직접 만든 패킷처럼 보입니다", "테더링 트래픽이 폰 자체 활동과 **구별되지 않는다**"가 5곳에 반복된다. **IP/TCP 계층에서만** 참이다. 페이로드는 그대로 지나가므로 PC 브라우저의 TLS ClientHello 지문(JA3/JA4), User-Agent, HTTP/2 SETTINGS, OS별 트래픽 패턴은 남는다. "구별되지 않는다"는 검증되지 않은 단정이므로 "IP·TCP 헤더 수준에서는 구별되지 않는다"로 범위를 좁혀야 한다. |
| 3.2 | **L** | `README.md:26` | "(TTL, TCP 옵션, MSS 등 모두 정상)" — 근거 제시 없이 나열. 실측했다면 어떻게 확인했는지 한 줄, 아니면 "추정"임을 표시. |
| 3.3 | **L** | `README.md:44` | "TetherService.kt # 포그라운드 서비스, **세 컴포넌트** 수명 관리" — 셋이 무엇인지(socks/http/hotspot) 명시되지 않아 파일이 늘어난 지금은 세는 기준이 모호하다. |
| 3.4 | — | `PeerFilter.kt:53–55`, `Socks5Server.kt:262–264` | 참고로 이 두 곳은 **모범적**이다. 미검증 가정과 남은 빈틈을 명시적으로 표시했다. 위 3.1도 같은 방식으로 다루면 된다. |

## 4. 보안 결함

| # | 심각도 | 위치 | 내용 |
|---|---|---|---|
| 4.1 | **H** | `WifiHotspot.kt:163–174` | `createGroup`이 `BUSY`로 실패하면 **기존 P2P 그룹을 그대로 채택하고 `onResult(true)`** 를 보고한다. 그 그룹의 패스프레이즈가 사용자 값과 같은지 확인하지 않는다. 결과적으로 UI 입력란에는 새로 생성한 116비트 패스프레이즈가 보이는데 실제 무선망은 **다른(모르는) 자격증명**으로 떠 있다. 지속(persistent) 그룹이나 **다른 앱이 만든 그룹**까지 흡수할 수 있고, 이후 `stop()`이 남의 그룹을 내려버린다. SSID/패스프레이즈가 일치하는지 확인할 수 없다면 재사용하지 말고 실패로 보고하는 것이 맞다. |
| 4.2 | **M** | `windows-wifi.bat` 자동 승격부 | `Start-Process -FilePath '%~f0' -Verb RunAs`에 `-ArgumentList`가 없어 **인자를 잃는다.** 비관리자 상태에서 실행하는 정상 경로에서는 `README.md:119`가 안내하는 `windows-wifi.bat 1085`가 조용히 1080으로 되돌아간다. 잘못된 포트로 붙어 실패하거나, 최악의 경우 1080을 점유한 다른 프로세스에 붙는다. |
| 4.3 | **M** | `TetherService.kt:58–65` + `HttpProxyServer.kt:82–95` | HTTP 프록시에는 `lastError`가 없고 `proxyError`는 SOCKS5에서만 채운다(`:55`). 8282–8291이 전부 막히면 UI는 이유 없이 `—`만 보여준다(`MainActivity.kt:146`). "포트를 조용히 옮기지 않고 실패를 알린다"는 이번 변경의 원칙이 HTTP 쪽에는 적용되지 않았다. |
| 4.4 | **M** | `DestinationFilter.isBlocked` | **`255.255.255.255`(제한 브로드캐스트)가 통과한다** — any-local/loopback/link-local/site-local/multicast 어디에도 걸리지 않고 `a != 0`, `a != 100`. Java `DatagramSocket`은 SO_BROADCAST가 기본 on이므로 UDP 릴레이(`Socks5Server.kt:363–366`)가 폰이 붙어 있는 LAN으로 브로드캐스트를 뿌릴 수 있다. 함께 빠진 것: IPv4-compatible IPv6(`::a.b.c.d`)와 NAT64 `64:ff9b::/96` — `asIpv4()`는 `::ffff:` 형태만 정규화한다(`IpAddress.kt:22–25`). |
| 4.5 | **M** | `TetherService.kt:77` | 두 바인딩이 모두 실패해도 `isRunning = true`라서 UI는 "Running"을 표시한다(`MainActivity.kt:131`). 게다가 실패한 서버 인스턴스를 그대로 캐시하므로(`:56`, `:64`) 이후 `onStartCommand`가 재시도하지 않는다 — 사용자가 Stop→Start를 해야 벗어난다. |
| 4.6 | **L** | `AndroidManifest.xml:5,9,13` | `WAKE_LOCK`, `ACCESS_NETWORK_STATE`, `CHANGE_NETWORK_STATE`를 선언했지만 **코드에서 전혀 쓰지 않는다**(`WakeLock`/`PowerManager`/`ConnectivityManager` 참조 0건). 불필요한 권한은 공격면이자 심사 리스크다. 부수 효과로, 웨이크락이 없으므로 화면이 꺼지면 릴레이 성능이 떨어질 수 있다 — 현재 CPU를 붙잡는 유일한 장치가 `MainActivity.kt:39`의 `FLAG_KEEP_SCREEN_ON`이고 그건 액티비티가 보일 때만 유효하다. 권한을 지우든, 실제로 웨이크락을 쓰든 한쪽으로 정리해야 한다. |
| 4.7 | **L** | `macos-wifi.sh:35` | `cleanup()`이 `networksetup -setdnsservers Wi-Fi Empty`로 **사용자가 직접 설정한 Wi-Fi DNS를 지운다.** 이 스크립트는 DNS를 건드리지 않는다(tun2proxy가 `--dns-addr`로 TUN에만 적용). 스크립트가 만들지 않은 상태를 되돌리는 정리 코드다. |
| 4.8 | **L** | `ConnectionRegistry.kt:52–61` | `closeAll()`이 스냅샷 후 `clear()`한다. `executor.shutdownNow()`가 폐기한 대기 태스크의 소켓은 `finally`를 실행할 주인이 없어 **등록 해제도 close도 되지 않은 채** 남는다. `clear()` 대신 실제 close 성공분만 제거하거나, 스냅샷 이후 재확인이 필요하다. |

## 5. 비효율 / 유지보수성

| # | 심각도 | 위치 | 내용 |
|---|---|---|---|
| 5.1 | **M** | `WifiHotspot.kt:32–33`, `177–186` + `TetherService.kt:97` | `manager.initialize()`로 만든 `WifiP2pManager.Channel`을 **한 번도 닫지 않는다.** `stop()`은 `removeGroup`만 호출하고, `TetherService`는 ON마다 새 `WifiHotspot`을 만든다(실패한 시도까지 포함) → **핫스팟 on/off 1회당 Channel 1개 누수.** `Channel`은 API 27부터 `AutoCloseable`이므로 minSdk 26 기준 버전 가드와 함께 `stop()`에서 닫아야 한다. |
| 5.2 | **M** | `Socks5Server.kt:360`, `:431` | `parseUdpHeader`가 ATYP=3(도메인) 데이터그램마다 `InetAddress.getByName()`을 **단일 `relayClientToRemote` 스레드에서 동기 호출**한다. 캐시도 타임아웃도 없어 느린 조회 하나가 그 association의 **모든** UDP 트래픽을 막는다. 최소한 짧은 TTL 캐시나 조회 전용 실행자가 필요하다. |
| 5.3 | **M** | `HttpProxyServer.kt:374–396` | `readHeaderLine`이 raw `SocketInputStream`에서 **1바이트씩 `read()`** 한다 → 헤더 1KB당 syscall 약 1000회. 본문 바이트를 스트림에 남겨야 하는 제약 때문인데, `BufferedInputStream`으로 감싸고 **그 스트림을 `relay()`에도 넘기면** 제약을 지키면서 해결된다(현재는 `relay()`가 `client.getInputStream()`을 다시 호출해 버퍼 내용을 잃는다). |
| 5.4 | **L** | `Socks5Server.kt:475`, `HttpProxyServer.kt:336` | `t.join(5_000)` 하드코딩. `Socks5Server`에는 같은 값의 `RELAY_JOIN_TIMEOUT_MS`(`:537`)가 이미 있는데 UDP 경로에서만 쓰인다. `HttpProxyServer`에는 상수가 없다. |
| 5.5 | **L** | `HttpProxyServer.kt:290` | `HOP_BY_HOP_HEADERS.any { it.equals(k, true) }` — 헤더당 리스트 선형 순회. 소문자 키 `Set` 조회로 바꾸면 의도도 더 분명해진다. |
| 5.6 | **L** | `app/build.gradle.kts:30–32` | `viewBinding = true`인데 생성 클래스를 쓰는 곳이 없다(`MainActivity`는 `findViewById` 6회). 불필요한 코드 생성이다 — 끄거나 실제로 쓰거나. |
| 5.7 | **L** | `strings.xml:3` + `AndroidManifest.xml:22` | `app_name`이 정의만 되어 있고 매니페스트는 `android:label="USB Tether"`로 하드코딩. 같은 문자열이 `TetherService.kt:157`, `:172`에도 또 하드코딩되어 총 3중 관리다. |
| 5.8 | **L** | `Socks5Server.kt:486`, `HttpProxyServer.kt:347` | `pipe()`가 모든 예외를 `catch (_: Exception) {}`로 완전히 삼킨다. 정상 종료와 실제 오류가 구분되지 않아 릴레이 문제를 진단할 수 없다. 최소한 `Log.v` 수준의 흔적은 남길 만하다. |
| 5.9 | **L** | `WifiHotspot.kt:228–231` | SSID 길이·문자 검증이 없다. `DIRECT-UT-` 접두사까지 포함해 32바이트를 넘기면 `WifiP2pConfig.Builder`가 던지는 예외가 일반 메시지 "Invalid SSID/passphrase"로만 보인다(`:78`). 입력 시점에 길이를 막는 편이 낫다. |
| 5.10 | **L** | `MainActivity.kt:94` + `HotspotPreferences.kt:20` | 빈 SSID도 그대로 저장되고, 이후 `ssid()`가 기본값이 아니라 `""`를 돌려준다 → `TetherService.kt:88`에서 거부. **퀵 설정 타일 경로는 사용자가 앱을 열어 SSID를 다시 넣을 때까지 계속 실패한다.** `save()`에서 공백을 걸러야 한다. |
| 5.11 | **L** | `TetherService.kt:109` + `WifiHotspot.kt:182` | `removeGroup(ch, null)`의 결과를 버리고 곧바로 `hotspotActive = false`를 게시한다. 제거가 실패해도 UI·타일은 "off"를 표시하므로, 그룹이 살아 있는데 꺼진 것으로 보일 수 있다. |

---

## 우선 조치 권고

1. **`CLAUDE.md:32`의 "probes ports 1080–1089" 삭제** — 같은 문서 `:68`과 정면 충돌하며, 하필 이번 변경이 없앤 탈취 경로를 다시 정설로 만든다.
2. **`WifiHotspot.reuseExistingGroup` 재검토**(4.1) — 사용자가 보는 패스프레이즈와 실제 무선망 자격증명이 갈릴 수 있다.
3. **`ConnectionRegistry` KDoc의 register 위치 수정**(1.1) — 오해하고 되돌리면 스레드 폭주가 재발한다.
4. **`CLAUDE.md:28`의 툴체인 버전 전면 갱신**(2.2) + Kotlin 버전 고정 여부 결정.
5. **`windows-wifi.bat` 승격 시 인자 전달**(4.2), **macOS 자동 승격 주석/코드 일치**(1.5).
6. **`WifiP2pManager.Channel` 닫기**(5.1).
7. **`README.md`의 없는 APK·macOS 바이너리 안내 정정**(2.4, 2.5), 문서에 **퀵 설정 타일 추가**(2.7).
