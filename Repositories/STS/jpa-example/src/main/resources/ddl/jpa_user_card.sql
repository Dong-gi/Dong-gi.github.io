create table jpa_user_card1 (
    user_card_id bigserial primary key,
    user_id bigint,
    card_id bigint
);
create table jpa_user_card2 (
    user_card_id bigserial primary key,
    user_id bigint,
    card_id bigint
);