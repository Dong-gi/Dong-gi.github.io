# Dong-gi.github.io 작업 지침

Pug → 정적 HTML 개인 블로그. `pugs/**/*.pug` 가 `posts/**/*.html` 로 렌더된다.
**이 파일이 저장소의 유일한 지침 문서다.** 규격·절차·인계 문서를 따로 두지 않는다.

---

## 저장소 구조

**손으로 쓰는 것**

| 경로 | 무엇 |
|---|---|
| `pugs/` | **모든 글의 원본.** 카테고리별 `.pug`. 글을 고칠 때 건드리는 유일한 곳 |
| `figures/` | 그림을 만드는 **JS 생성기**(`<과목>-<블록>.mjs`)와 그 산출 SVG |
| `d2/` | 상자·화살표 도식의 **D2 소스**와 산출 SVG |
| `source/` | 빌드 스크립트, 레이아웃(`skeleton.pug`), CSS/JS, 색인 데이터 |
| `imgs/` | 사진·스크린샷. **`build-img` 가 여기서 제자리 변환한다**(아래 이미지 절) |
| `Repositories/` | 글에서 인용하는 코드 원본. `+codeBtn('/Repositories/…')` 가 연다 |
| `files/` | 첨부 파일과 사이트맵 |
| `index.pug` | 첫 화면 원본 |
| `scripts/` | 품질 검사와 도구 |

**빌드가 만들고 커밋되는 것 — 손으로 고치지 마라**

| 경로 | 언제 갱신되는가 |
|---|---|
| `posts/` | `npm run build-pug` |
| `index.html`, `404.html` | `npm run build-pug` |
| `figures/<과목>/*.svg` · `d2/**/*.svg` | `build-figure` · `build-d2` (소스와 같은 폴더) |
| `source/posts-compressed.json`, `files/sitemap.txt` | `build-pug` |
| `fonts/` | 수식 폰트는 고정. 일본어 폰트는 문서가 새 한자를 쓸 때 |

---

## 빌드

```bash
npm ci
npm run build         # 요소 다섯 개를 병렬로. 콘솔에는 성패만
npm run build-pug     # 요소 하나만. build-figure / build-d2 / build-img / build-js 도 같은 꼴
```

빌드는 **요소별로 나뉘어 있고**, 요소 스크립트와 산출물이 모두 `source/build/` 에 있다.

```
source/build/pug.ts                   요소 스크립트
source/build/build-pug.log            그 요소의 로그 (git 무시)
source/build/build-pug-sha.json       그 요소의 내용 해시 기록 (커밋한다)
```

sha 는 (정규화 경로 → 내용 해시) 기록이고, 빌드는 이것과 견줘 **바뀐 것만** 다시 만든다.
어떤 요소를 통째로 다시 만들려면 **`source/build/build-<요소>-sha.json` 을 지운다.**

**요소 사이에 순서는 없다** — 다섯이 모두 동시에 돌고 서로의 산출물을 읽지 않는다.
**`img` 만 해시 파일이 없다.** 원본을 제자리에서 바꾸므로 이미 바뀐 것은 다시 안 건드린다.

Node 24 이상. 도식 렌더에는 `d2` 바이너리가 필요하고, 없으면 그 요소를 통째로 건너뛴다
(해시를 남기지 않으므로 설치 후 다시 돌리면 만들어진다).

빌드 출력을 파일로 돌리지 마라(`npm run build > build.log`). 로그는 이미 요소마다 따로
남으므로 얻을 것이 없고, 요소가 실패하면 볼 곳은 `source/build/build-<요소>.log` 다.

수식은 **빌드 시점에 미리 렌더**된다(MathJax 4 CHTML + assistive MathML). 브라우저에서
MathJax 를 돌리지 않으므로 폰트는 `fonts/mathjax-newcm/` 에서 직접 서비스한다.

---

## 새 포스트를 추가할 때

1. `pugs/<카테고리>/<이름>.pug` 를 만든다.
2. **`source/posts.json` 에 항목을 등록한다.** 등록하지 않으면 렌더는 되지만
   사이드바 목록·사이트맵에서 빠진다.
3. 기초 과목이면 `package.json` 의 `check` 목록에도 더한다.
4. 그림 모듈을 새로 만들면 `source/build/figure.ts` 의 `SUBJECTS` 에 과목을 더한다.
   **거기 없는 접두어의 모듈은 조용히 무시된다.**
5. `pugs/` 아래에는 **`.pug` 만** 둔다. 다른 확장자는 렌더 대상에서 빠지고 경고가 난다.

---

## Pug 규약

- 들여쓰기 스페이스 4칸. 탭 금지.
- 모든 포스트는 `include ../../source/skeleton.pug` 후 `+post({ title, useMath })` 로 시작한다.
- 수식을 쓰면 `useMath: true`. **인라인 구분자는 `식[` … `]식`**, MathJax 는 `tags:'ams'`.
- 본문에서 부등호는 LaTeX 명령(`\lt \gt \le \ge`)이나 HTML 엔티티(`&lt;` `&gt;`)로 쓴다.
- 산문은 반드시 태그(`p`, `li` …) 아래 두거나 `|` 로 시작한다. 평문으로 시작한 줄은 태그로 해석된다.
- 굵게는 `<b>...</b>`. `**굵게**` 는 pug 가 처리하지 않아 별표가 화면에 그대로 찍힌다.
- **인자를 이스케이프하는 믹스인이 넷이다 — `+ths` · `+tds` · `+asA`, 그리고 `+w3img` 의
  캡션.** `<b>` 나 `&#34;` 를 넣으면 태그가 화면에 그대로 찍힌다. 따옴표가 필요하면 `‘ ’`
  를 쓴다. `+w3img` 캡션은 같은 문자열이 `alt` 로도 들어가므로 화면 낭독기가 태그를 읽고,
  `식[...]식` 도 조판되지 않는다. 표 칸에는 평문과 `식[...]식` 만 넣고 역슬래시는 두 번 쓴다:
  `+tds('규칙', '식[\\log(xy) = \\log x + \\log y]식')`.
  **수식이나 강조가 필요한 표는 `tr`/`td` 를 직접 써라.**
- 믹스인을 산문 안에서 `#[+example(...)]` 처럼 부르면 예제 카운터가 올라간다.
  링크(`+goto`, `+asA`)와 앵커(`+pos`) 외에는 인라인으로 부르지 마라.

### 주요 믹스인 (`source/skeleton.pug`)

| 믹스인 | 용도 |
|---|---|
| `+post(options)` | 문서 골격. `{ title, useMath }` |
| `+example(label?)` | 예제 블록. 번호 자동. `id="example-N"` 앵커를 남긴다 |
| `+solution(title?, open?)` | 풀이. 기본 접힘. `+solution('증명', true)` 로 펼침 |
| `+table()` / `+ths(...)` / `+tds(...)` | 표 |
| `+asA(href, text?)` | 외부 링크 |
| `+asCode(lan, title?)` / `+asInlineCode(code, lan?)` | 코드 |
| `+codeBtn(options)` | 외부 코드 파일 열기 버튼 |
| `+pos(name)` / `+goto(name, href?)` | 앵커와 링크 |
| `+bookInfo(options)` | 참고 도서 카드 |
| `+w3img(src, description?)` | 그림 |
| `+jp(text, reading?)` | 일본어 |

**예제 번호를 손으로 적지 마라.** `+example` 이 문서마다 1부터 자동으로 붙인다.
중간에 끼워 넣어도 뒤 번호를 고칠 필요가 없게 한 장치다.

`+goto(name, href)` 는 `href#pos<해시>` 로 가는 링크를 만들고, 목적지에 `+pos(name)` 이
있어야 한다. **헤딩만 있고 `+pos` 가 없으면 링크가 빈 곳으로 떨어지는데 브라우저는 아무
오류도 내지 않는다.** `npm run check-links` 로 확인한다.

- **문서 안의 절**을 가리키면 `+goto('절 이름', '/posts/….html')`. 목적지에 `+pos` 를 둔다.
- **문서 전체**를 가리키면 `+asA('/posts/….html', '문서 이름')`. 제목으로는 앵커를 두지 않는다.

---

## 이미지

`build-img` 는 `imgs/` 의 사진을 **webp·avif·원본 가운데 가장 작은 것**으로 바꿔
**같은 자리에 `.webp` 이름으로 덮어쓴다**(폭이 2000 을 넘으면 2000 으로 줄인다).
변환본을 따로 두는 폴더가 없다 — 저장소를 불리지 않으려는 것이다.

그래서 `+w3img` 는 **`/imgs/` 로 시작하는 경로의 확장자를 `.webp` 로 바꿔 건다.**
문서에는 `.png`·`.jpg` 로 적어도 되고 실제로 걸리는 것은 `.webp` 다.
`npm run check` 의 `그림 전부 존재` 항목도 같은 규칙으로 찾는다.

**이름이 `.webp` 라고 내용까지 webp 인 것은 아니다.** 가장 작은 것을 고르므로 avif 나
원본 그대로가 그 이름으로 들어 있는 경우가 많다(현재 103 개 중 실제 webp 는 1 개).
브라우저는 바이트를 보고 판단하므로 `<img>` 로는 문제가 없다.

`/figures/`·`/d2/` 의 SVG 는 이 규칙이 걸리지 않는다.

### 외부에서 가져온 사진

`figures/` 와 `d2/` 는 **선과 도형만** 만든다. 그래서 **겉모습 자체가 정보인 자리**
(현미경 사진, 간섭무늬, 스펙트럼)만 사진을 쓴다. 셋을 모두 갖춰야 한다.

1. **겉모습이 곧 자료다.** 도식으로 그리면 오도하거나 빠뜨리는 것이 생기는 자리
2. **라벨 없이 읽힌다.** 라이선스가 개작을 막으면 영어 라벨을 한국어로 못 바꾼다.
   축과 눈금에 영어가 붙은 그래프 이미지는 쓰지 않는다 — `figures/` 로 다시 그린다
3. **라이선스를 파일마다 실제로 확인했다.** **퍼블릭 도메인·CC0·CC BY 만 쓴다.**
   CC BY-SA 와 CC BY-NC 는 쓰지 않는다. 애매하면 쓰지 않는다

**자격이 없는 것** — 인물 사진과 초상, 실험 기구 사진, 이미 생성 그림이 잘 하는 자리,
"있으면 보기 좋을" 것, 스크린샷.

**반드시 내려받아 `imgs/<과목>/` 에 둔다.** 외부 URL 핫링크는 하지 않는다.
출처는 캡션이 아니라 장 끝 `h2 이 장의 출처` 에 **TASL**(제목·저작자·출처 링크·라이선스)로
모아 적는다. 퍼블릭 도메인도 적는다. 사진에서는 **무엇이 실제 색이고 무엇이 입힌 색인지**
캡션에서 밝힌다. 흰 배경 사진은 어두운 화면에서 튄다 — 같은 값이면 검은 배경을 고른다.

(저작권 판단은 법률 자문이 아니다.)

---

## 일본어

`+jp` 는 **본문에 일본어를 적는 유일한 방법이다.** 문서의 `lang` 이 `ko-KR` 이고 한자는
한중일이 같은 코드포인트를 쓰면서 자형이 다르므로(한자 통합), 맨몸으로 적으면 그 자리만
한국 자형으로 그려진다. Noto Sans CJK 에서 실제로 갈리는 것이 CJK 통합한자 20,976 자 중
**630 자(3.0%)** 이고 흔한 한자에 몰려 있다 — `社 者 海 具 半 化 類 漢 食 分`.

`+jp` 가 `lang="ja"` 를 붙이고 `source/default.css` 의 `[lang="ja"]` 가
`fonts/japanese/` 의 서브셋 폰트를 건다. 폰트에 없는 글자는 **경고 없이** 폴백하므로
`npm run check` 가 `fonts/japanese/COVERAGE.txt` 와 견줘 잡는다. 새 한자를 쓰면
`scripts/subset-japanese-font.py` 로 서브셋을 다시 만든다.

**그림 안에는 한자를 쓰지 마라.** 생성 SVG 는 `<img>` 로 실려 본문의 `lang` 을 물려받지
못하므로 자형이 읽는 사람의 폰트에 달린다. 가나는 지역별 자형 치환이 없어 안전하다.

---

## 그림

| 종류 | 어디에 | 산출 |
|---|---|---|
| 좌표계·곡선·벡터·기하 | `figures/<과목>-<블록>.mjs` | `figures/<과목>/<이름>.svg` |
| 상자와 화살표(흐름·분류·구조) | `d2/<과목>/<이름>.d2` | 같은 경로의 `.svg` |

`figures/lib.mjs` 가 축·곡선·화살표·라벨을 만드는 최소 라이브러리다. 의존성이 없고
다크 모드를 SVG 내부 미디어 쿼리로 처리한다. **모듈은 장마다 하나씩 두어라** — 여럿이
동시에 작업할 때 같은 파일을 고치게 되고, 그림 이름은 과목 안에서 유일해야 한다.

```js
import { svg, frame, px, txt, legend, PALETTE } from './lib.mjs';
export default [{ name: 'math-calc-tangent', svg: svg({ width, height, title, body }) }];
```

**알려진 함정.**

- **라벨에 수식을 쓸 수 없다**(`<img>` 라 MathJax 가 닿지 않는다). 아래첨자는 `v~0`,
  `A~{xy}` 표기가 `<tspan>` 으로 내려간다. 그리스 문자는 유니코드로
- **HTML 엔티티도 쓸 수 없다.** `lib.mjs` 의 `esc` 가 `&` 를 다시 이스케이프한다.
  따옴표는 `‘ ’` 를 쓴다
- 그 `~` 치환 때문에 **`45~71%` 같은 물결표가 아래첨자로 먹힌다.** 라벨에서 `~` 를 피하라
- **`px()` 에 `cls: 'ark'`·`'ar1'` 을 넘기면 선이 사라지고 화살촉만 남는다** —
  그 CSS 클래스가 SVG 안에 없다. 회색 화살표는 `cls: 'ax'` 를 쓴다.
  (모듈이 자기 `arw()` 를 정의해 `stroke` 를 직접 주는 방식도 있다. 그 경우는 안전하다)
- `svg()` 는 `<style>` 에 `text{font-size:13px}` 를 박는다. 크기는 속성이 아니라
  인라인 `style="font-size:…"` 로 준다
- 색은 `PALETTE` 만 쓴다. **색만으로 구분하지 말고 항상 이름을 함께 적어라**
- **라벨끼리 겹치는지 눈으로 확인하라.** 브라우저로 렌더해 보는 것이 유일하게 믿을 만하다
- 좌표가 화폭을 넘지 않는지 확인하라. 테두리를 두른 노드는 여백을 넉넉히 둔다

`.d2` 는 상자와 화살표 전용이다(좌표 그래프에 쓰지 마라). `d2` 바이너리가 없는 환경에서는
같은 것을 `figures/` 의 상자·화살표로 그려도 된다.

---

## `pugs/fundamental` 집필 기준

목표는 **문서 하나로 기초부터 학사 과정 수준까지 자습이 가능**한 것이다.
읽는 사람에게 가정하는 것은 **중학교 수학까지**이고, 그 위의 도구는 필요한 자리에서
그때그때 도입한다.

### 1. 정의 없이 쓰지 않는다 — 예외 없다

**어떤 기호·단위·용어도 정의되기 전에 등장할 수 없다.**

- 기호는 **처음 쓰는 문장에서** 이름·뜻·단위를 함께 밝힌다
- 단위는 **무엇으로 정의되는지**를 함께 준다. 낯선 단위는 익숙한 것으로 바꿔 쓴다
- 표를 앞세워 "나중에 나올 것"을 몰아 보여주지 않는다

### 2. 수학은 필요한 순간에 도입한다

가정해도 되는 것은 사칙연산, 비례와 반비례, 일차함수와 그래프, 간단한 연립방정식,
피타고라스 정리, 원의 넓이, 기초 확률까지다. **삼각함수·지수로그·벡터·극한·미분·적분은
가정하지 않는다.** 필요해지면 셋 중 하나로 처리한다 — 그 자리에서 필요한 만큼만 설명하거나,
기초수학 문서로 연결하거나(링크를 따라가야만 이해되는 구조로 만들지는 마라), 피한다.

### 3. 예제는 생각의 흐름을 보인다

각 절(`h2`)마다 `+example` 을 최소 둘 두고 전부 `+solution` 을 붙인다.
풀이는 답에 이르는 계산이 아니라 **왜 그 식을 고르는지**부터 적는다.

**무엇을 묻는가 → 무엇을 아는가 → 어느 관계를 쓰는가 → 계산 → 답이 말이 되는가.**

문제 자체가 무의미해 보이면 **문제를 고친다.**

### 3-2. 예제의 네 유형

한 절에 둘 이상을 두되 유형을 섞는다.

| 유형 | 묻는 것 | 풀이가 갖춰야 할 것 |
|---|---|---|
| **계산형** | 값 | 위 다섯 단계 그대로 |
| **판단형** | 여러 후보 중 무엇을 고를지 | 고른 근거 + **탈락한 후보가 왜 탈락했는지 전부** |
| **서술형** | 논증·비판·설명 | 모범답안 + **반드시 들어가야 하는 것** + **다르게 답해도 되는 범위** |
| **선택형** | 지식 확인 | **오답 하나하나가 왜 오답인지** |

**선택형은 정답만 적으면 안 된다.** "정답: 3번"으로 끝나는 풀이는 아무것도 가르치지 않는다.
**서술형의 풀이는 정답표가 아니라 채점 기준이다** — 혼자 읽는 사람은 자기 답이 "틀렸는지
다른지"를 판별할 수단이 없다. **합의되지 않은 내용은 선택형·단답형으로 묻지 않는다.**

### 3-3. 합의되지 않은 내용을 쓰는 법

갈린다는 사실을 숨기면 문서가 거짓말을 하고, "여러 견해가 있다"로 끝내면 독자가 아무것도
얻지 못한다. 셋으로 구분해 표시한다.

| 표시 | 언제 | 형태 |
|---|---|---|
| **정설** | 사실상 다툼이 없다 | 그냥 서술한다. 표시하지 않는다 |
| **다수설·소수설** | 갈리지만 무게가 다르다 | 다수설을 먼저 쓰고 **소수설과 그 근거를 반드시 잇는다** |
| **미결** | 어느 쪽도 우세하지 않다 | 나란히 놓는다. **저자가 고르지 않는다** |

1. **비율을 숫자로 적지 않는다.** 꼭 쓰려면 조사 이름·연도·기관·표본 크기를 같은 문장에
   적고 그것이 설문 결과일 뿐임을 밝힌다
2. **반대 견해는 가장 강한 형태로 적는다.** 반박하기 쉬운 형태로 요약해 놓고 무너뜨리는
   것은 독자를 속이는 것이다
3. **미결을 결론인 척하지 않는다.** 저자의 선호가 있으면 선호라고 적는다

### 3-4. 출처

사실 주장·인용·특정 학자의 견해에는 출처를 단다. 원전이 번역·개작을 허락하지 않으면
**읽고 새로 쓰되 링크로 가리킨다.** 문장을 옮기지 않는다.
**확인하지 못한 것은 확인하지 못했다고 적는다. 추측이면 추측이라고 적는다.**

### 4. 그림을 아끼지 않는다

말로 세 문장 이상 설명해야 하는 공간·관계·변화는 그림으로 만든다.
**캡션은 그림 제목이 아니라 그림에서 읽어내야 할 것을 적는다.**
나쁨: "포물체 궤적". 좋음: "최고점에서 0이 되는 것은 수직 속도뿐이고 수평 속도는 그대로다".

### 5. 장의 시작과 끝

각 `h1` 장은 **이 장에서 답할 질문** 한두 개를 평범한 말로 열고, **앞 장에서 가져오는
것**이 있으면 명시한다. 끝에는 **정리** 표를 둔다.
문서 앞에 `이 문서를 읽는 방법`·`참고문서`, 뒤에 `더 공부할 것`을 둔다.

### 6. 문체

- 간결한 어투를 유지한다. 쉽게 쓴다는 것이 장황하게 쓴다는 뜻은 아니다
- 한국어 용어를 먼저 쓰고 괄호에 영어를 병기한다. **처음 한 번만**
- 독자를 낮춰 부르지 않는다. "쉽죠?", "간단합니다" 는 쓰지 않는다
- **어려운 부분은 어렵다고 말한다.** "여기가 이 장에서 가장 걸리는 곳이다" 는 유용하다

### 여럿이 나눠 쓸 때

장을 나눠 쓰면 **인계 목록(원장)을 임시로 만들어 쓴다** — 범위, 장 구성, 용어 규약,
기호 배정, 합의되지 않은 항목 목록, 그림 규약. **그것이 유일한 계약이다.**
장을 끝낸 사람은 "이 장이 새로 도입한 것"과 "원장과 어긋나게 정한 것"을 보고한다.
**작업이 끝나면 원장은 저장소에 남기지 않는다.**

### 단위 정책

서술·예제·풀이의 모든 수치는 **SI 단위와 SI 허용 단위만** 쓴다.

- SI 허용 단위(BIPM SI 백서 9판 Table 8)는 `min h d ° ′ ″ ha L t Da eV au Np B dB` 가 전부다.
  `°C` 는 SI 유도단위이므로 제한 없이 쓴다
- **야드·파운드계 금지** — ft, lb, psi, °F, mile, inch, gallon, hp, Btu. 변환 문제도 만들지 않는다
- 목록에 없는 비SI 단위는 대체한다: atm/bar/mmHg/Torr → `Pa`·`kPa`, cal/kcal → `J`·`kJ`,
  Å → `pm`·`nm`, rpm → `rad/s`·`rev/min`, 몰농도 M → `mol/L`,
  기체상수는 `8.314 J/(mol·K)`, 표준압력은 `100 kPa`(IUPAC 표준상태)
- 단위를 쓰는 문서는 앞부분에 `h3 SI 허용 단위와, 다른 책에서 만나는 비SI 단위` 절을
  **한 번만** 두고 환산표를 정리한다. 그 표 바깥에서는 비SI 단위를 언급하지 않는다.
  형식은 `pugs/fundamental/physics.pug` 의 같은 절을 따른다

**환산표는 단위가 붙은 수치를 다루는 문서만 둔다.** 물리·화학·생물이 그렇고, 논리학·철학·
심리학·일본어·인과추론에는 그런 수치가 없어 두지 않는다. 검사도 없는 것을 통과로 본다.

물리 상수 등 수치는 CODATA/SI 기준값을 쓴다. **확신할 수 없는 값은 기호로 남기고
출처가 불분명한 주장은 넣지 않는다.**

---

## 확인 명령

```bash
node scripts/check-chapter.mjs <장 조각.pug>   # 장 조각 9가지 검사
npm run check                                   # 기초 과목 문서 13가지 검사
npm run check-links                             # +goto 앵커가 실재하는가 (빌드 뒤에)
npm run verify                                  # 실제 브라우저로 404·수식·그림·가로 넘침

# 수식 구분자 짝 확인 (두 수가 같아야 한다)
f=pugs/fundamental/physics.pug; grep -o '식\[' $f | wc -l; grep -o '\]식' $f | wc -l
```

`npm run check` 의 열셋 가운데 마지막 둘은 일본어 검사(폰트 서브셋이 문서를 덮는가,
`+jp` 밖에 일본어가 있는가)여서 `+jp` 를 쓰는 문서에만 돌고, 나머지 열하나는 모든 문서에 돈다.

**`npm run check-links` 는 `posts/dev/web/html.html` 에서 오탐 2건(`#ref`·`#here`)을 낸다.**
그 문서가 HTML 앵커를 **설명하는** 글이라 산문 속 예시를 링크로 읽는 것이다.
고칠 것이 아니다 — 다른 문서에서 새로 뜨는 것만 보면 된다.

**`npm run verify` 는 지금 이 저장소에서 바로 돌지 않는다.** `playwright` 가
`devDependencies` 에 없고, `scripts/verify-pages.mjs` 가 크로미움 경로를 리눅스 기준인
`/opt/pw-browsers/chromium` 으로 박아 두었다. 쓰려면 `npm i -D playwright` 와
`npx playwright install chromium` 을 거친 뒤 그 `executablePath` 를 지워야 한다.

---

## 진행 상황 (2026-09)

`pugs/fundamental/` **열세 문서 전부 자습서 규격**을 따른다. 합계 **67,699줄, 예제 2,748개,
그림 참조 1,554건**. 생성 SVG 1,198개(모듈 109개)와 d2 도식 356개.

| 문서 | 줄 | 문서 | 줄 |
|---|---|---|---|
| 이산수학(mcs) | 7,643 | 물리 | 5,181 |
| 철학 | 6,335 | 화학 | 5,096 |
| 일본어 | 6,148 | 생물 | 5,088 |
| 논리학 | 5,997 | 알고리즘 | 4,895 |
| 심리학 | 5,673 | 확률 | 3,859 |
| **인과추론** | 5,197 | 기초수학 | 3,849 |
| | | 선형대수 | 2,738 |

mcs 는 원서(MIT 6.042) 22개 장 전부를 다루며 이 사이트에서 가장 큰 문서다.

**과목마다 규격을 넓혀 온 자취가 있다.**

- **철학** 때문에 §3-2(예제의 네 유형)·§3-3(합의되지 않은 내용)·§3-4(출처)가 생겼다.
  전문가들 사이에 합의가 없는 과목을 쓰려면 서술과 예제에 규칙이 더 필요했다
- **심리학** 때문에 §3-5(외부에서 가져온 사진)가 생겼다. 뇌 해부처럼 그릴 수 없는 자리가
  있었고, 라이선스를 파일마다 확인해야 했다
- **일본어**는 첫 어학 문서다. **소리와 필순을 다루지 않는다** — 쓸 만한 음성 자료를
  확보하지 못했고 작성 도구가 소리를 검증할 수도 없기 때문이다. `+jp` 와 서브셋 폰트,
  그림 안 한자 금지가 여기서 나왔다
- **인과추론**은 확률·이산수학·심리학·철학 위에 얹힌다. 확률 문서가 `더 공부할 것` 에
  목차를 미리 적어 둔 자리를 채운 것이다. 모든 방법을 **가정 / 식별 / 의심** 세 조각으로
  적고, 실제 논쟁 중인 인과 주장을 예제의 정답으로 쓰지 않는다

**어학 문서나 합의가 갈리는 과목을 새로 쓸 때는 위 자취를 먼저 읽어라.** 규격이 왜
지금 모양인지가 거기 있다.
