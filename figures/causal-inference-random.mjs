/**
 * 무선배정 장 그림. 원장 §5.2 의 규약을 따른다.
 *
 *   - 처치는 왼쪽, 결과는 오른쪽
 *   - 열린 화살표는 실선
 *   - 노드 이름은 짧은 한국어. SVG 안에는 수식을 쓸 수 없다
 *
 * 표 모양 그림(관측된 칸과 물음표 칸, 배정별 값)은 인과 그래프가 아니지만,
 * 칸과 글자를 같은 규약으로 그리려고 graph-lib 의 panel·tag·caseTitle 을 쓴다.
 */
import { svg } from './lib.mjs';
import { node, edge, tag, caseTitle, panel } from './causal-inference-graph-lib.mjs';

/* 본문의 가상 표와 같은 숫자다. 화분 넷, 처치했을 때와 하지 않았을 때의 줄기 길이. */

/* ── 1. 비어 있는 칸에 값을 빌려온다 ─────────────────────────── */

const borrow = caseTitle(310, 36, '한 줄에 한 칸씩 비어 있다')
    + caseTitle(200, 72, '처치했을 때')
    + caseTitle(430, 72, '하지 않았을 때')
    + panel(40, 88, 545, 112)
    + tag(95, 120, '처치를 받았다')
    + caseTitle(200, 120, '보인다')
    + panel(392, 102, 76, 28)
    + tag(430, 120, '?')
    + tag(95, 178, '받지 않았다')
    + panel(162, 160, 76, 28)
    + tag(200, 178, '?')
    + caseTitle(430, 178, '보인다')
    + edge(430, 164, 430, 124, { from: 0, to: 0, tone: 's2' })
    + tag(510, 148, '여기서 빌려온다')
    + caseTitle(310, 232, '이 화살표 하나가 이 장의 전부다');

/* ── 2. 딱지를 맞바꿔도 같은 무리인가 ────────────────────────── */

const exchange = caseTitle(165, 40, '실제로 한 배정')
    + panel(35, 52, 260, 130)
    + node(105, 92, '무리 ㄱ') + tag(105, 140, '처치')
    + node(225, 92, '무리 ㄴ') + tag(225, 140, '대조')
    + caseTitle(475, 40, '딱지를 맞바꾼 배정')
    + panel(345, 52, 260, 130)
    + node(415, 92, '무리 ㄱ') + tag(415, 140, '대조')
    + node(535, 92, '무리 ㄴ') + tag(535, 140, '처치')
    + edge(300, 117, 340, 117, { from: 0, to: 0 })
    + caseTitle(320, 212, '딱지를 바꿔 붙여도 이야기가 달라지지 않는다면')
    + caseTitle(320, 234, '두 무리는 서로 바꿔 놓아도 되는 무리다');

/* ── 3. 무엇이 처치를 정하는가 ───────────────────────────────── */

const RS = 52, RC = 28, RX = 26;

const coin = caseTitle(165, 42, '성질이 정할 때')
    + panel(20, 52, 290, 190)
    + node(165, 96, '대상의 성질', { rx: RS })
    + node(72, 196, '처치', { role: 'x' })
    + node(258, 196, '결과', { role: 'y' })
    + edge(165, 96, 72, 196, { from: 40, to: RX })
    + edge(165, 96, 258, 196, { from: 40, to: RX })
    + edge(72, 196, 258, 196, { from: RX, to: RX, tone: 's1' })
    + caseTitle(475, 42, '동전이 정할 때')
    + panel(330, 52, 290, 190)
    + node(475, 96, '대상의 성질', { rx: RS })
    + node(372, 96, '동전', { rx: RC })
    + node(382, 196, '처치', { role: 'x' })
    + node(568, 196, '결과', { role: 'y' })
    + edge(372, 96, 382, 196, { from: RX, to: RX, tone: 's1' })
    + edge(475, 96, 568, 196, { from: 40, to: RX })
    + edge(382, 196, 568, 196, { from: RX, to: RX, tone: 's1' })
    + caseTitle(320, 268, '차이는 성질에서 처치로 들어가는 화살표 하나다');

/* ── 4. 두 방법이 닿는 범위 ──────────────────────────────────── */

const reach = caseTitle(310, 36, '무선배정이 닿는 범위는 어디까지인가')
    + panel(60, 50, 500, 160)
    + caseTitle(310, 74, '대상이 가진 성질 전부')
    + panel(105, 92, 210, 100)
    + caseTitle(210, 134, '연구자가 잰 성질')
    + tag(440, 145, '연구자가 재지 못한 성질')
    + edge(210, 252, 210, 184, { from: 0, to: 0, tone: 's2' })
    + tag(210, 268, '재서 맞추는 방법')
    + edge(470, 252, 470, 202, { from: 0, to: 0, tone: 's1' })
    + tag(470, 268, '무선배정');

/* ── 5. 배정만 바꾼다 ────────────────────────────────────────── */

const reassignRow = (lx, vx, rows) => rows
    .map(([y, name, value]) => tag(lx, y, name) + caseTitle(vx, y, value)).join('');

const reassign = caseTitle(170, 40, '가와 나에 처치')
    + panel(30, 52, 280, 162)
    + reassignRow(100, 230, [
        [84, '처치군 관측', '9 와 8'],
        [116, '대조군 관측', '4 와 4'],
        [154, '순진한 비교', '4.5'],
        [186, '선택 편향', '2'],
    ])
    + caseTitle(490, 40, '가와 다에 처치')
    + panel(350, 52, 280, 162)
    + reassignRow(420, 550, [
        [84, '처치군 관측', '9 와 5'],
        [116, '대조군 관측', '6 과 4'],
        [154, '순진한 비교', '2'],
        [186, '선택 편향', '0'],
    ])
    + caseTitle(330, 240, '화분도 잠재결과도 그대로다. 배정만 바뀌었다')
    + caseTitle(330, 260, '평균처치효과는 두 경우 다 2 다');

/* ── 6. 여섯 배정에서 나온 여섯 값 ───────────────────────────── */

const DRAWS = [
    [130, 100, '가와 나', '순진한 비교 4.5'],
    [330, 100, '가와 다', '순진한 비교 2'],
    [530, 100, '가와 라', '순진한 비교 2.5'],
    [130, 180, '나와 다', '순진한 비교 1.5'],
    [330, 180, '나와 라', '순진한 비교 2'],
    [530, 180, '다와 라', '순진한 비교 -0.5'],
];

const sixDraws = caseTitle(330, 42, '넷 가운데 둘을 고르는 여섯 가지 배정')
    + DRAWS.map(([x, y, name, value]) => panel(x - 90, y - 32, 180, 64)
        + caseTitle(x, y - 6, name) + tag(x, y + 18, value)).join('')
    + caseTitle(330, 238, '하나하나는 2 가 아닌데 여섯의 평균이 2 다');

/* ── 7. 양수성 ───────────────────────────────────────────────── */

const positivity = caseTitle(175, 40, '동전으로 정했을 때')
    + panel(30, 52, 290, 148)
    + tag(100, 88, '자리 ㄱ') + caseTitle(232, 88, '처치 2 · 대조 2')
    + tag(100, 124, '자리 ㄴ') + caseTitle(232, 124, '처치 2 · 대조 2')
    + caseTitle(175, 176, '어느 자리에서도 양쪽이 다 있다')
    + caseTitle(495, 40, '관찰한 자료였을 때')
    + panel(340, 52, 290, 148)
    + tag(410, 88, '자리 ㄱ') + caseTitle(542, 88, '처치 4 · 대조 0')
    + tag(410, 124, '자리 ㄴ') + caseTitle(542, 124, '처치 0 · 대조 4')
    + caseTitle(485, 176, '어느 자리에도 견줄 상대가 없다')
    + caseTitle(330, 228, '오른쪽에서는 자료를 늘려도 빈 칸이 채워지지 않는다');

/* ── 8. 배정과 실제 처치가 갈린다 ────────────────────────────── */

const RA = 46, RP = 52;

const noncompliance = node(88, 92, '무선 배정', { rx: RA })
    + node(320, 92, '실제 처치', { role: 'x', rx: RA })
    + node(556, 92, '결과', { role: 'y' })
    + node(150, 212, '대상의 성질', { rx: RP })
    + edge(88, 92, 320, 92, { from: RA, to: RA, tone: 's1' })
    + edge(320, 92, 556, 92, { from: RA, to: RX, tone: 's1' })
    + edge(150, 212, 320, 92, { from: RP, to: RA })
    + edge(150, 212, 556, 92, { from: RP, to: RX })
    + tag(204, 76, '일부만 따른다')
    + tag(455, 185, '성질이 결과도 바꾼다')
    + caseTitle(330, 262, '무선인 것은 배정이고 결과를 바꾸는 것은 실제 처치다');

export default [
    {
        name: 'ci-r-borrow',
        svg: svg({
            width: 620, height: 265,
            title: '두 칸짜리 표에서 관측된 칸의 값을 비어 있는 칸으로 옮기는 화살표',
            desc: '처치를 받은 줄은 하지 않았을 때 칸이 물음표이고, 받지 않은 줄의 관측된 값에서 그 칸으로 화살표가 올라간다',
            body: borrow,
        }),
    },
    {
        name: 'ci-r-exchange',
        svg: svg({
            width: 640, height: 250,
            title: '같은 두 무리에 처치 딱지를 서로 바꿔 붙인 두 배정',
            desc: '왼쪽에서는 무리 ㄱ 이 처치이고 오른쪽에서는 무리 ㄴ 이 처치다',
            body: exchange,
        }),
    },
    {
        name: 'ci-r-coin',
        svg: svg({
            width: 640, height: 290,
            title: '성질이 처치를 정하는 경우와 동전이 처치를 정하는 경우',
            desc: '왼쪽에서는 대상의 성질에서 처치와 결과로 화살표가 둘 다 나가고, 오른쪽에서는 처치로 들어오는 화살표가 동전에서 오는 것 하나뿐이다',
            body: coin,
        }),
    },
    {
        name: 'ci-r-reach',
        svg: svg({
            width: 620, height: 285,
            title: '대상이 가진 성질 전부를 두르는 큰 칸과 그 안에 잰 성질을 두르는 작은 칸',
            desc: '아래에서 화살표 둘이 올라오는데 하나는 안쪽 칸에서 멈추고 하나는 바깥 칸까지 간다',
            body: reach,
        }),
    },
    {
        name: 'ci-r-reassign',
        svg: svg({
            width: 660, height: 270,
            title: '같은 화분 넷에서 배정만 바꾼 두 경우의 값',
            desc: '왼쪽은 가와 나에 처치했을 때, 오른쪽은 가와 다에 처치했을 때의 관측 평균과 순진한 비교와 선택 편향',
            body: reassign,
        }),
    },
    {
        name: 'ci-r-six-draws',
        svg: svg({
            width: 660, height: 250,
            title: '넷 가운데 둘을 고르는 여섯 가지 배정과 각각에서 나온 순진한 비교',
            desc: '여섯 칸에 배정과 값이 하나씩 적혀 있고 값은 마이너스 0.5 에서 4.5 까지 흩어져 있다',
            body: sixDraws,
        }),
    },
    {
        name: 'ci-r-positivity',
        svg: svg({
            width: 660, height: 250,
            title: '두 자리 모두에서 양쪽이 나온 배정과 자리마다 한쪽만 있는 자료',
            desc: '왼쪽은 자리 ㄱ 과 자리 ㄴ 에 처치와 대조가 둘 다 있고, 오른쪽은 자리 ㄱ 이 전부 처치이고 자리 ㄴ 이 전부 대조다',
            body: positivity,
        }),
    },
    {
        name: 'ci-r-noncompliance',
        svg: svg({
            width: 640, height: 285,
            title: '무선 배정에서 실제 처치를 거쳐 결과로 가는 길과 성질에서 들어오는 화살표',
            desc: '왼쪽 무선 배정에서 가운데 실제 처치로 화살표가 가고, 아래쪽 대상의 성질에서 실제 처치와 결과로 화살표가 둘 더 들어온다',
            body: noncompliance,
        }),
    },
];
